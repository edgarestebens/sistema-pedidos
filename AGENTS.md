# Brasas del Sur — Angular + Supabase

Stack: Angular 19 (standalone) + Tailwind CSS v4 + `@supabase/supabase-js`.

Prefer existing patterns in `src/app/` (services with signals, feature components, `core/` types/mappers).
